const { perms, normal } = require('../../utils');

module.exports = {
	name: '',
	description: '',
	aliases: [''],
	args: false,
	usage: '[]',
    cooldown: 5,
    guildOnly: true,
	perms: perms.BOTOWNER,
    async execute(bot, msg, args) {

    }
}